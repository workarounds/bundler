<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.workarounds.samples.bundler">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher" android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="in.workarounds.samples.bundler.BookListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="in.workarounds.samples.bundler.BookDetailActivity" />

        <service android:name=".ToastService"/>

    </application>

</manifest>
